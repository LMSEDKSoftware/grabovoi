{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Flutter Auto Run",
            "type": "shell",
            "command": "pkill -f 'flutter run' && sleep 2 && flutter run -d chrome --web-port=8080",
            "group": "build",
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            },
            "problemMatcher": []
        }
    ]
}
