# Voz numérica (0-9)

Añade en cada carpeta (`male/` y `female/`) los archivos:

- `0.mp3`, `1.mp3`, … `9.mp3`

Requisitos: MP3 cortos, volumen homogéneo, sin click al inicio/fin.
La app los usa para leer la secuencia dígito a dígito durante el pilotaje.
